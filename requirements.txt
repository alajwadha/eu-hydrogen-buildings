# eu-hydrogen-buildings — Python dependencies
# Install: pip install -r requirements.txt
# Python 3.10+
#
# Tested with: numpy 2.4.6, pandas 3.0.3, scipy 1.17.1, matplotlib 3.11.1,
# SALib 1.5.2. The results in the manuscript were produced under these versions;
# the lower bounds below are the minimum that expose the APIs the code uses.

pandas>=1.5.0
numpy>=1.23.0,<3            # scripts call np.trapezoid with an np.trapz fallback
scipy>=1.10.0       # scipy.stats.rankdata — standardized rank-regression sensitivity
requests>=2.28.0
# 3.11, not 3.6: the figure scripts call Patch.set_hatchcolor and
# set_hatch_linewidth to ink hydrogen's hatch independently of the edge colour,
# which several figures use as a bar separator or outline. Both are 3.11 APIs.
matplotlib>=3.11.0
SALib>=1.4.7        # variance-based (Sobol) global sensitivity of 2050 demand
PyYAML>=6.0
pyarrow>=12.0.0     # parquet I/O — required by the country-build pipeline (and MC samples)

# Geospatial — required for the country-build pipeline (code/scripts/country_build/)
# and the EUBUCCO integration across all 29 countries.
geopandas>=0.13.0
shapely>=2.0.0
fiona>=1.9.0
pyogrio>=0.7.0      # faster GeoJSON reader; geopandas uses it when available

# Optimisation solver — COST_OPT least-cost LP (code/src/Optimisation.py).
# PuLP bundles the CBC solver, so no separate solver install is needed.
pulp>=2.7.0

# Word/PDF twin builder (paper/ae_submission/build.py). Documented in that folder's
# REPRODUCE.md but previously absent here, so `python build.py` worked only where these
# happened to be installed already.
pypandoc_binary>=1.13   # bundles pandoc; LaTeX -> .docx via citeproc
weasyprint>=60.0        # the PDF engine build.py drives through pandoc
